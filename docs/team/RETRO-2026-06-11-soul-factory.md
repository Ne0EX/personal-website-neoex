# Factory Retro · 2026-06-11 · first all-real-numbers run

> Phase 4 of TASK-2026-06-10-SOUL-FACTORY. Every number below traces to `.harness/factory/build/dashboard.json` (rebuild: `node scripts/factory/collect.mjs`) or a signature/ndjson event on disk. View it live: `node scripts/factory/serve.mjs` → http://localhost:4173/

## Snapshot (backfill era, ~2026-05-15 → 2026-06-11)

| metric | value | trace |
|---|---|---|
| agent runs | 211 | signatures corpus (214 sigs, 13 STATUS tasks unsigned) |
| TASK tickets | 199 (194 in-flight · 5 shipped) | STATUS ∪ signatures |
| tokens (upper bound) | 93.3M | transcript metadata, session-shared across sibling tasks |
| cost (upper bound) | ~$132.4k-equivalent at tracker pricing | same caveat — treat as ceiling, not spend |
| gates | 30 (29 approved · 1 revised · 0 blocked) | Algol audits + rail events |
| flagged | 1 — BATCH-AUDIT-2026-05-16 · Privacy · 10 issues | the 437-carry-over signature class, since fixed by auto-baseline (Phase 0.1) |
| autonomy rate | 100% agent-gated | human-gate extraction from STATUS markers not yet landing in tickets — see debts |
| reasoning tokens | 0 recorded | not present in backfill metadata |
| fidelity | 206/206 runs `backfill-low`, all token attribution `estimated` | first `full` events start with the next signed task (sign-work tail hook) |

## What the data already says

1. **The signature-coverage denominator is honest now:** 13/19 recent STATUS tasks have no signature (audit-signature-completeness WARN). Autonomy/coverage metrics are only as good as this numerator — closing the gap is the cheapest fidelity win.
2. **47 stale batons** (audit-stale-handoffs WARN) — almost all `next_recipient: Algol` without a pickup signature. Either Algol signs per-audit going forward (it did for SOUL-FACTORY-0) or we declare the baton convention changed.
3. **ROI panels are honest blanks.** baselineHours/mitigated/humanMinutes are null by policy. If Peat wants the dual-ROI view live, the cheapest path is per-task baseline-hours estimates at assignment time (one frontmatter field in the handoff).

## Debts created/confirmed this run (all projected in STATUS)

- per-run token windows once auto-baseline-era data accrues; human-gate events not landing in tickets[].gates (Procyon, 2026-06-24)
- sign-work should exclude gitignored runtime outputs from files_touched (SF1-Procyon FILE_HASH_DRIFT was the collector's own outputs regenerated by the tail hook) (Canopus, 2026-06-20)
- production lint debt 61 errors → Sirius 2026-06-18 · axiom projection → 2026-06-24 · multi-owner signing stash-dance → Canopus 2026-06-20

## Program state

Phase T ✓ tiering codified · Phase 0 ✓ seams shut · Phase 1 ✓ collector live · Phase 2 ✓ dashboard over real data (browser-verified) · Phase 3 OS-lane awaits Peat after he tours the dashboard · Phase 4 = this document, generated from the artifact it reports on.
