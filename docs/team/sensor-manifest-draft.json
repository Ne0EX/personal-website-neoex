{
  "$schema": "https://worldline.local/sensor-manifest.schema.json",
  "manifest_version": "0.1-draft",
  "generated": "2026-06-01",
  "generated_by": "polaris · α-OPS-00 (synthesis of zerotrust-cert-design control map)",
  "status": "DESIGN-ONLY — build requires Peat approval (see BUILD PLAN)",
  "substrate": {
    "decision": "revive worldline-harness/ engine + merge with active .claude/hooks + .harness/ axioms into ONE system",
    "name_collision": "TODAY two things are named 'worldline-harness': (a) .harness/worldline-harness.config.json (active rail registry, owner Canopus) and (b) worldline-harness/.harness/ (dormant ported engine). COLLAPSE: keep .harness/ as the single live config+axiom+rail root; rename the dormant engine dir to .harness/engine/ and fold its config into the existing rail registry. One config root, one rail vocabulary.",
    "install": ["harness:check / harness:sensors / harness:judges npm scripts", ".git/hooks/pre-commit symlink -> .harness/engine/adapters/git/pre-commit.sh", "wire dormant secrets-scan + mutating-action sensors into settings.json PreToolUse Bash matcher"],
    "adapt": ["farm-erp anchors -> Worldline anchors (intent/design-rails/tech-rails already exist as curated/; repoint to soul-atlas + axioms-v1)", "farm-erp fixtures -> Worldline fixtures (globe, soul-atom gallery, content routes)"],
    "drop": ["codex + copilot-cli adapters (Worldline is Claude Code only)", "farm-erp-specific phase-scope rules", "regulated-industry/enterprise judges not justified for a solo garden"]
  },
  "barrier_test": "Every rail at mode=block MUST carry barrier_class=HARD or a signed FRICTION-ONLY waiver. Every LLM-judge rail MUST be mode in {shadow,warn} unless thresholded:true + threshold set (Constitutional-Classifiers exception). Enforced by audit-rail-barrier-class.sh (the spine sensor).",
  "counts": {
    "total_controls": 17,
    "deterministic": 10,
    "judge_required": 2,
    "hybrid": 4,
    "na_scope": 1
  },
  "sensors": [
    { "key": "audit-rail-barrier-class", "control": "impossible-not-tedious (spine)", "class": "DETERMINISTIC", "barrier": "HARD", "mode": "block", "owner": "canopus", "exists": false, "spec": "parse rail registry; every mode=block rail asserts barrier_class=HARD or signed waiver; every judge rail asserts mode in {shadow,warn} unless thresholded. FAIL CLOSED.", "net_new": true },
    { "key": "audit-permissions-nonempty", "control": "least-privilege (deny-by-default)", "class": "DETERMINISTIC", "barrier": "HARD", "mode": "block", "owner": "canopus", "exists": false, "spec": "FAIL CLOSED if .claude/settings.json permissions{} is empty or lacks a deny default. Pairs with populating settings.json deny-by-default + per-agent allow-list (config, not script).", "highest_leverage": true },
    { "key": "settings-permissions-config", "control": "least-privilege + least-agency", "class": "DETERMINISTIC", "barrier": "HARD", "mode": "block", "owner": "canopus", "exists": false, "spec": "config change: populate permissions with deny Bash(curl:*)/Bash(wget:*)/net-egress; Write/Edit scoped to web paths; allow-list per agent.", "note": "not a sensor — the config the sensor guards" },
    { "key": "audit-least-agency-config", "control": "least-agency / tool-misuse", "class": "DETERMINISTIC", "barrier": "HARD", "mode": "block", "owner": "canopus", "exists": false, "spec": "assert curl/wget denied + a blocking PreToolUse Bash hook present; FAIL CLOSED on regression." },
    { "key": "wire-mutating-action", "control": "tool/resource misuse", "class": "DETERMINISTIC", "barrier": "HARD", "mode": "block", "owner": "canopus", "exists": "dormant", "spec": "WIRE existing .harness/engine/core/sensors/mutating-action.ts + mutating-bash.json denylist as a real settings.json PreToolUse Bash hook (exit-1 fail-closed). Promotion, not invention.", "note": "single highest-leverage Part-II win" },
    { "key": "wire-secrets-scan", "control": "credential/output leak", "class": "DETERMINISTIC", "barrier": "HARD", "mode": "block", "owner": "canopus", "exists": "dormant", "spec": "WIRE secrets.ts regex scan (sk-ant-, sk-, JWT, ghp_, env-file-tracked, ANTHROPIC_API_KEY-in-client) into pre-commit + PostToolUse. FAIL CLOSED." },
    { "key": "audit-main-web-only", "control": "blast-radius / assume-breach", "class": "DETERMINISTIC", "barrier": "HARD", "mode": "block", "owner": "canopus", "exists": false, "spec": "pre-merge on PRs targeting main: allowlist web-production path globs; FAIL CLOSED if any staged path is outside it (.claude/, .harness/, docs/team, worldline-harness/, soul-atlas master all blocked from main)." },
    { "key": "audit-single-source", "control": "config-integrity / blast-radius", "class": "DETERMINISTIC", "barrier": "HARD", "mode": "block", "owner": "canopus", "exists": false, "bug": "globe-3-divergent-blobs", "spec": "for single-source artifacts (globe component, soul-atom tokens) assert exactly ONE canonical definition; prototype/handoff copies import it or are sha256-identical. FAIL CLOSED on divergence." },
    { "key": "audit-claim-vs-ground-truth", "control": "always-verify / ground-truth-observed", "class": "HYBRID", "barrier": "HARD", "mode": "block", "owner": "algol", "exists": false, "bug": "build-green-200-passed-as-done", "spec": "Stop + pre-commit: for closing TASK signature, re-run rails named in steps[], recompute files_sha256 of files_touched; FAIL CLOSED on mismatch or empty steps[]/no-baseline." },
    { "key": "audit-render-fidelity-vs-intent", "control": "render-fidelity-vs-intent", "class": "HYBRID", "barrier": "HARD", "mode": "shadow->block", "owner": "algol", "exists": false, "bug": "archive-shipped-crooked-overlay", "spec": "Playwright render of the actual production route -> compare layout signature (bounding boxes / transform / overflow) against design-intent manifest. DETERMINISTIC pre-gate catches crooked/overlay/offscreen (transform!=identity, overlap, clipping); JUDGE only on residual aesthetic drift, shadow-mode.", "judge_residue": true },
    { "key": "audit-ground-truth-observed", "control": "ground-truth-observed (DoD)", "class": "DETERMINISTIC", "barrier": "HARD", "mode": "block", "owner": "algol", "exists": false, "bug": "nobody-opened-the-page", "spec": "TASK cannot close DONE unless a screenshot+ack artifact exists for each shipped route: .claude/visual-diffs/<task>/observed/<route>.png + observed.json with route, sha256, viewport, human/agent ack. FAIL CLOSED on missing artifact. Proxy becomes anchor." },
    { "key": "audit-search-index-completeness", "control": "search-index-completeness", "class": "DETERMINISTIC", "barrier": "HARD", "mode": "block", "owner": "algol", "exists": false, "bug": "pagefind-indexed-2-of-15", "spec": "post-build: assert pagefind index page_count == crawlable route count (enumerate app routes / sitemap); FAIL CLOSED on mismatch. Also assert no data-pagefind-body under display:none." },
    { "key": "audit-tool-param-shape", "control": "direct prompt injection", "class": "HYBRID", "barrier": "HARD", "mode": "block", "owner": "canopus", "exists": false, "spec": "WIRED PreToolUse hook: tool params match a JSON schema (max-length caps, no shell-metachar in path args, deny unknown tool). FAIL CLOSED. Judge residue: free-text intent, shadow." },
    { "key": "wrap-untrusted-fetch", "control": "indirect/cross-domain prompt injection", "class": "HYBRID", "barrier": "HARD", "mode": "shadow", "owner": "canopus", "exists": false, "spec": "PreToolUse on WebFetch/WebSearch/external-Read: tag returned content PROVENANCE:UNTRUSTED (non-overridable delimiter) + enforce domain allowlist (extend runtime-allowlist.json into a gate). Judge only on flagged spans, shadow.", "judge_residue": true },
    { "key": "audit-mcp-manifest-pin", "control": "tool poisoning / rug-pull", "class": "DETERMINISTIC", "barrier": "HARD", "mode": "block", "owner": "canopus", "exists": false, "spec": "session-start + ConfigChange: hash each enabled MCP server's tool list/descriptions, diff vs .harness/mcp-manifest-lock.json; FAIL CLOSED on drift." },
    { "key": "audit-lockfile-drift", "control": "supply-chain (model+tool)", "class": "DETERMINISTIC", "barrier": "HARD", "mode": "block", "owner": "algol", "exists": false, "spec": "CI: assert package-lock.json unchanged unless package.json changed in same commit; optional OpenSSF-Scorecard threshold on direct deps. FAIL CLOSED on silent transitive drift." },
    { "key": "scope-waivers", "control": "regulated-industry / enterprise N/A", "class": "N/A", "barrier": "N/A", "mode": "n/a", "owner": "peat-signs", "exists": false, "spec": ".harness/scope-waivers.json signed by Peat listing HSM/TPM identity, mTLS cert-pinning, SIEM streaming, governance committee, HIPAA/FINRA/GDPR/FedRAMP as explicitly OUT-OF-SCOPE for a solo garden; audit-rail-barrier-class.sh treats their absence as intentional." }
  ],
  "judges_minimal": [
    { "key": "render-fidelity-aesthetic-residue", "why_not_deterministic": "deterministic pre-gate catches geometry (crooked/overlap/clip); 'does it FEEL like the frozen design' aesthetic drift is genuinely subjective", "starts": "shadow", "sensor": "audit-render-fidelity-vs-intent" },
    { "key": "indirect-injection-intent", "why_not_deterministic": "provenance tag + domain allowlist quarantine the bulk deterministically; reading the INTENT of an already-quarantined untrusted span needs an LLM", "starts": "shadow", "sensor": "wrap-untrusted-fetch" }
  ],
  "build_waves": [
    { "wave": 0, "name": "substrate + spine", "owner": "canopus", "items": ["collapse worldline-harness name collision", "harness:* npm scripts", "pre-commit symlink", "audit-rail-barrier-class.sh + barrier_class rail-schema field", "scope-waivers.json (Peat signs)"], "checkpoint": "peat" },
    { "wave": 1, "name": "least-privilege + least-agency (root gap)", "owner": "canopus", "items": ["settings.json deny-by-default + per-agent allow-list", "deny curl/wget/net-egress", "audit-permissions-nonempty.sh", "wire mutating-action + secrets-scan as PreToolUse Bash gate", "audit-least-agency-config.sh"], "checkpoint": "peat" },
    { "wave": 2, "name": "today's-bug sensors", "owner": "algol", "items": ["audit-ground-truth-observed.sh", "audit-render-fidelity-vs-intent.sh (det pre-gate; judge shadow)", "audit-search-index-completeness.sh", "audit-single-source.sh", "audit-claim-vs-ground-truth.sh"], "checkpoint": "peat" },
    { "wave": 3, "name": "blast-radius + supply-chain", "owner": "canopus+algol", "items": ["audit-main-web-only.sh", "audit-mcp-manifest-pin.sh", "audit-lockfile-drift.sh", "ConfigChange hook on settings.json/.harness/*.json"], "checkpoint": "peat" },
    { "wave": 4, "name": "judge promotion (only where no det equivalent)", "owner": "algol", "items": ["audit-tool-param-shape.sh PreToolUse", "wrap-untrusted-fetch.sh + provenance tagging", "keep both judges shadow; promote to warn only after fixture validation"], "checkpoint": "peat" }
  ]
}
