# TO · Canopus
# FROM · Polaris
# TASK · TASK-2026-05-14-01
# TYPE · SPEC
# CREATED · 2026-05-14T00:00:00Z

---

## scope

ลงทะเบียน GENESIS roster ทั้ง 9 ใบให้ Claude Code harness รู้จัก เป็น subagents เรียกได้ด้วย `subagent_type: <codename>` ปัจจุบัน harness ไม่ register agent ใดเลย — ทดสอบ `subagent_type: "vega"` ได้ error `Agent type 'vega' not found`. รากของปัญหาคือไฟล์ `.claude/agents/*.md` ทุกใบไม่มี YAML frontmatter (`name`, `description`, optional `model`) ที่ harness ใช้ในการ register

ผลข้างเคียงที่ Peat สังเกตในระบบ harness ภายนอก session นี้: เมื่อไม่มี frontmatter ระบุ model harness บางครั้ง auto-deploy Polaris เป็น subagent ด้วย Sonnet (ขัดกับ persona spec ที่ระบุ "Opus, max thinking — non-negotiable"). การเพิ่ม `model: opus` ใน frontmatter ของ persona ที่ต้องการ Opus จะปิด drift นี้ด้วยหินก้อนเดียว

## PRD reference

ไม่มี PRD เฉพาะสำหรับ harness wiring — อ้าง `.claude/AGENTS.md` (nomenclature, roster), `.claude/agents/<codename>.md` (persona, model spec), `docs/team/FILE-OWNERSHIP.md` (territory)

## slices

### S1 · Canopus (α-HRN-07) · เพิ่ม frontmatter ในไฟล์ persona ทั้ง 9

input
- ไฟล์ปัจจุบันที่ `.claude/agents/{polaris,vega,sirius,altair,procyon,betelgeuse,arcturus,algol,canopus}.md`
- Claude Code subagent frontmatter spec — รูปแบบมาตรฐาน:
  ```yaml
  ---
  name: <lowercase-codename>
  description: <when this agent should be invoked — used by main agent to route>
  model: <opus | sonnet | inherit>   # optional
  tools: <comma-separated>            # optional, defaults to all
  ---
  ```

output
- frontmatter block หัวไฟล์ทุกใบ คงเนื้อ prose เดิม **ทุกบรรทัด** ไม่ตัด ไม่เรียบเรียงใหม่
- `name` = lowercase codename ตรงกับชื่อไฟล์ (polaris, vega, sirius, …)
- `description` = ประโยคเดียวอธิบายว่าเมื่อใดควรเรียก agent นี้ ดึง keyword จาก §identity + §territory ของแต่ละ persona — ตัวอย่าง Polaris: "Product Manager · decomposes Peat's directives into parallel slices, assigns 3–5 agents per task, tracks status, reviews acceptance. Invoke for orchestration, task assignment, cross-agent dispute resolution. Never invoke for code/prose/design implementation."
- `model` — ตามที่ persona ระบุใน §model:
  - polaris → `opus` (persona บังคับ "Opus, max thinking — non-negotiable")
  - canopus → `sonnet` (persona ระบุ "Sonnet. Default thinking effort.")
  - ที่เหลือ → อ่าน §model ในแต่ละไฟล์ ถ้าระบุชัด ใช้ตามนั้น ถ้าไม่ระบุ ใช้ `inherit` (ไม่ตั้ง = ตามตัว main session)
- ไม่ตั้ง `tools` ในรอบนี้ ปล่อย default ทั้งหมด

acceptance
1. `Agent({subagent_type: "<codename>"})` เรียกได้สำเร็จทั้ง 9 ชื่อ — Polaris จะ verify ด้วยการเรียก smoke test ทุกใบหลังรับงาน
2. ไฟล์ทุกใบยังมี content ส่วน prose ครบทุก section ตามเดิม — diff prove เป็น **append-only frontmatter** ไม่มีบรรทัดอื่นถูกลบหรือแก้
3. `description` ของทุกใบยาวประมาณ 1–3 ประโยค บอกชัดว่า "เมื่อใดควรเรียก" และ "เมื่อใดไม่ควรเรียก" — เพราะ harness ใช้ field นี้ในการ route subagent
4. `model` ใน Polaris = `opus` (ปิด drift ที่ Peat รายงาน)

start · now

### S2 · Canopus · update FILE-OWNERSHIP.md

input
- `docs/team/FILE-OWNERSHIP.md` ปัจจุบันไม่ระบุเจ้าของ `.claude/agents/*.md` ทำให้ task นี้เกิด territory ambiguity (ดู §territory note ด้านล่าง)

output
- เพิ่มบรรทัดในส่วนที่เกี่ยวข้อง:
  - **persona file frontmatter** (`.claude/agents/<codename>.md` หัวไฟล์ YAML) → Canopus (harness registration)
  - **persona file prose** (body หลัง frontmatter) → ตัว agent นั้นๆ (self-owned), Vega prose sign-off เหมือนเดิม

acceptance
1. ใครเปิดไฟล์ FILE-OWNERSHIP.md แล้วเห็นทันทีว่า persona file มี 2 ชั้น เจ้าของต่างกัน
2. ไม่ขัดกับ canopus.md §what I do not touch ("Persona files — Polaris owns those.") — Canopus เปิด PROPOSED REVISION handoff ไปแก้บรรทัดนั้นใน canopus.md ด้วย ให้สอดคล้องกับ FILE-OWNERSHIP.md ใหม่

start · หลัง S1 — S1 คือทางวิ่ง critical, S2 คือ cleanup ของระเบียบ

## dependencies

- S2 WAIT(S1) — เพราะ S2 อ้างถึงการแบ่ง "frontmatter vs prose" ที่เพิ่งสร้างขึ้นใน S1
- ไม่มี dependency กับ task อื่น

## parallelism plan

S1 → S2 sequential (S2 พึ่งโครงสร้าง S1 — แต่ S2 เล็กมาก ใช้เวลา <5 นาที)

## non-goals

- **ไม่แก้ persona prose** — ทุก section หลัง frontmatter ห้ามแตะ
- **ไม่ปรับ tone, ไม่เพิ่ม section ใหม่, ไม่ตัดข้อความเก่า**
- **ไม่ rename codename, ไม่แตะ designation (α-OPS-00 ฯลฯ)**
- **ไม่แตะ CREW.md, ไม่แตะ portrait paths, ไม่แตะ visual reference links**
- **ไม่เพิ่ม `tools` field** ในรอบแรก — รอบนี้ทำ register-only

## territory note (สำหรับ Canopus)

`.claude/agents/*.md` ไม่มีเจ้าของชัดใน FILE-OWNERSHIP.md ปัจจุบัน Canopus's persona ระบุว่า "Persona files — Polaris owns those" แต่ Polaris's territory section ก็ไม่ได้ claim ไฟล์เหล่านี้ — เป็น ownership gap

Polaris grant **one-time scoped permission** ให้ Canopus แก้ **frontmatter block หัวไฟล์เท่านั้น** ใน 9 ไฟล์นี้ สำหรับ task นี้ — ไม่ใช่ blanket permission persona body prose ยังเป็นของแต่ละ agent (Vega prose sign-off ตามเดิม) S2 จะทำให้กฎนี้เป็นทางการใน FILE-OWNERSHIP.md

## acceptance · ทั้ง task

- ทั้ง 2 slices signed + hook ผ่านครบ
- Polaris run smoke test ทุก codename — ทั้ง 9 invocations คืน response สำเร็จ
- STATUS.md (Polaris สร้างใหม่ถ้ายังไม่มี) บันทึก task closed
- diff ของ task เป็น **append-only frontmatter + 1 บรรทัดใน FILE-OWNERSHIP.md** เท่านั้น — ไม่มี content เก่าถูกลบหรือเปลี่ยน

## deadline

ไม่มี — Peat ไม่ระบุ ทำให้ดี ไม่ทำให้เร็ว

## known concerns Polaris ฝากไว้ล่วงหน้า

1. **subagent dispatch paradox** · TASK นี้ออกในขณะที่ผมยังเรียก `subagent_type: "canopus"` ไม่ได้ Polaris จะ dispatch ผ่าน `general-purpose` subagent แบบมี briefing ให้ทำตัวเป็น Canopus + อ้าง persona file นี้เป็น operating manual จนกว่า S1 จะเสร็จและ register สำเร็จ หลังจากนั้น future tasks ใช้ `subagent_type: "canopus"` ได้ตามปกติ
2. **description field ต้องเขียนให้ดี** · field นี้คือสิ่งที่ main agent อ่านเพื่อตัดสินใจ route — เขียนสั้นเกินไป → routing ผิด, เขียนยาวเกินไป → กลืน context window อ้างอิงตัวอย่างที่ Polaris ใส่ไว้ใน S1 ตามสไตล์นั้น
3. **model field กระทบ cost/latency** · Polaris = opus ตามที่ persona บังคับ ที่เหลือ ตามที่ persona spec ไว้ ห้ามตั้งเองนอกเหนือสิ่งที่ persona เขียน

---

*Polaris (α-OPS-00) — 2026-05-14*
