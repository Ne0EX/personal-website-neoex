# TASK-2026-05-15-21 · NETRA voice spec + audit-rail source-of-truth

> FROM · Polaris (α-OPS-00)
> TO · Arcturus (α-NET-05)
> DATE · 2026-05-15
> MODEL · sonnet
> SIZE · M

## assignment

Author the canonical NETRA voice spec within `lib/netra/**` so that:
1. Future NETRA prose (companion lines, refusal copy, streaming-token cadence) stays coherent
2. `scripts/audit-voice.sh` (currently STUB) can graduate to enforcing — Canopus will wire it after you ship the audit-detectable patterns

This TASK is voice REGISTER ONLY. Not the system prompt (TASK-51). Not the chat UI (Betelgeuse TASK-11-S2). Not tool definitions (TASK-51).

## read first

- `docs/prds/00-netra-character.md` (current character framing)
- `docs/team/DEV-PLAN-2026-05-15/01-tasks-by-prd.md` — TASK-21 scope
- `docs/design/journey-architecture.md` v1.2 §4 + companion line examples
- existing NETRA references — grep `app/`, `lib/`, `content/` for "NETRA" + "companion" + α-related instrument lines

## scope

Author `lib/netra/voice.md` containing:
- Vocabulary do/don't (allowed register, forbidden register)
- Sentence rhythm rules (companion-line length, refusal cadence, streaming-token chunking)
- Refusal register patterns
- α / divergence / observer-locus references — when used, how phrased
- Audit-detectable patterns: regex or token-set rules that `scripts/audit-voice.sh` will check

## constraints

- Stay within `lib/netra/**`
- Do NOT write the system prompt (TASK-51)
- Do NOT spec the chat UI (Betelgeuse TASK-11-S2)
- Do NOT define tools (TASK-51)

## harness

pre-task.sh Step 0 · sign-work.sh · return handoff to Polaris with audit-pattern enumeration ready for Canopus pickup.

## acceptance

`voice.md` exists with all 5 sections · audit-detectable patterns clearly enumerated · Canopus can wire `audit-voice.sh` from this spec without further questions.
