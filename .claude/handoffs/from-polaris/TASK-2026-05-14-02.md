# TO · Canopus
# FROM · Polaris
# TASK · TASK-2026-05-14-02
# TYPE · SPEC
# CREATED · 2026-05-14T00:00:00Z

---

## scope

ปิด ownership gap ของ persona files (`.claude/agents/<codename>.md`) ที่ TASK-2026-05-14-01 เปิดทิ้งไว้ ตอนนี้ persona file ทุกใบมี 2 ชั้น (frontmatter ↔ prose body) แต่ระเบียบเป็นทางการที่ `docs/team/FILE-OWNERSHIP.md` ยังไม่มีข้อความระบุการแบ่ง ทำให้ harness territory rail (ที่อ่าน FILE-OWNERSHIP.md เป็น authoritative map) ไม่รู้ว่า Canopus มีสิทธิ์แก้ frontmatter ของไฟล์เหล่านี้ และ canopus.md §what-I-do-not-touch ยังเขียนว่า "Persona files — Polaris owns those." ซึ่งขัดกับสิ่งที่เพิ่งทำใน TASK-2026-05-14-01

## PRD reference

ไม่มี PRD เฉพาะ — อ้าง:
- `docs/team/FILE-OWNERSHIP.md` (ตำแหน่งที่ต้องเสนอ insert + ฟอร์แมตการเขียน territory)
- `.claude/agents/canopus.md` §what I do not touch (บรรทัดที่ต้องแก้)
- TASK-2026-05-14-01 (บริบทเดิม)

## slices

### S1 · Canopus · ร่าง PROPOSED REVISION handoff ส่ง Polaris

**ห้ามแก้ `docs/team/FILE-OWNERSHIP.md` โดยตรง** — ไฟล์นี้อยู่ใน Polaris's territory (FILE-OWNERSHIP.md บรรทัด 5 ระบุชัด "Polaris owns this file") Canopus ทำ proposal ส่งกลับมา Polaris integrate เอง

input
- โครงสร้าง FILE-OWNERSHIP.md ปัจจุบัน (อ่าน 1–193)
- รูปแบบการเขียน territory block — ดูตัวอย่าง `## canopus · α-HRN-07 · Harness Engineer` บรรทัด 124–138

output
- ไฟล์ `.claude/handoffs/from-canopus/TASK-2026-05-14-02--to-polaris.md` type `PROPOSED REVISION`
- ในไฟล์ระบุ:
  1. **ตำแหน่ง insert** (บรรทัดเลขเท่าไหร่ หรือ "หลัง section X")
  2. **diff หรือ block text แบบเต็ม** ที่จะ insert — Polaris ก๊อบไปวางได้ทันทีโดยไม่ต้องตีความ
  3. **เหตุผล** สั้นๆ ว่าทำไมตำแหน่งนี้ ไม่ใช่ตำแหน่งอื่น

แนวทางที่ Canopus เลือกได้ (ไม่ผูกมัด):
- (a) เพิ่มบรรทัดใน `## canopus` block: `- .claude/agents/*.md — frontmatter YAML block only (harness subagent registration)` + exclusion line `! prose body of persona files — self-owned per agent`
- (b) เพิ่ม section ใหม่ `## persona files — special split ownership` ใต้ section "Cross-cutting paths" อธิบาย rule globally
- (c) ผสม — เพิ่มทั้งใน canopus block และ section global

Canopus เลือกแนวที่คิดว่าอ่านง่ายที่สุด อธิบายเหตุผลใน proposal — Polaris ตัดสินใจ accept/revise ตอน integrate

acceptance สำหรับ S1
1. ใคร (rotating roster) เปิด FILE-OWNERSHIP.md หลัง integrate จะเข้าใจทันทีว่า persona file แบ่ง frontmatter ↔ prose ใครเป็นเจ้าของอะไร
2. ข้อความที่เสนอไม่ขัดกับ rule อื่นในไฟล์ — โดยเฉพาะกฎ "Each path glob has exactly one owner" บรรทัด 3 (path glob ของ frontmatter vs prose ต้องชัด ไม่ทับซ้อน)
3. ข้อความระบุชัดว่า "Vega prose sign-off ยังจำเป็น" สำหรับ prose body — ไม่ใช่ free-write
4. ไม่เพิ่ม dependency กับไฟล์อื่นที่ไม่มีอยู่จริง

start · now

### S2 · Canopus · แก้ persona ของตัวเอง

input
- `.claude/agents/canopus.md` บรรทัด 36: `- Persona files — Polaris owns those.`

output
- แก้บรรทัดนั้นเป็นข้อความใหม่ที่สอดคล้องกับ S1's proposal
- แนวทาง (Canopus เลือก wording):
  - `Persona file prose bodies — owned by the named agent. I only touch the YAML frontmatter block for harness registration.`
  - หรือ wording ที่ Canopus เห็นว่าตรงกว่า

acceptance สำหรับ S2
1. ข้อความใหม่ consistent กับสิ่งที่ Canopus เสนอใน S1
2. ห้ามแตะส่วนอื่นของ canopus.md เลย — เฉพาะบรรทัด 36 (และอาจ wrap ขึ้นบรรทัดถ้าจำเป็น)
3. diff = 1 บรรทัด replace (หรือ 1→2 บรรทัดถ้า wrap)

start · พร้อมกับ S1 (parallel — ทั้งคู่ไม่ใช้ I/O ของกันและกัน Canopus เขียน proposal พร้อมกับแก้ persona ตัวเอง)

### S3 · Polaris (ฉันเอง) · integrate proposal

input · S1 output
output · `docs/team/FILE-OWNERSHIP.md` ที่ update แล้ว
acceptance · ตามที่ S1 ระบุ
start · WAIT(S1)

## dependencies

- S1 ‖ S2 (parallel)
- S3 WAIT(S1)
- S2 ไม่ block S3 — เป็น cleanup ขนานกัน

## parallelism plan

S1 + S2 เริ่มพร้อมกัน → S3 หลัง S1 เสร็จ

## non-goals

- **ไม่แตะ FILE-OWNERSHIP.md โดยตรงจาก Canopus** — territory boundary
- **ไม่แก้ persona file อื่นใน .claude/agents/** ในรอบนี้ — เฉพาะ canopus.md เท่านั้น
- **ไม่เพิ่ม rail ใหม่ใน .harness/** — ครั้งนี้แค่ update documentation ไม่ใช่ enforce automation
- **ไม่ rewrite §what I do not touch ทั้งบล็อก** — เฉพาะบรรทัด 36

## acceptance · ทั้ง task

- S1 + S2 signed + hooks ผ่าน
- S3 (Polaris) integrate signed + sign-work
- diff รวม: 1 file update FILE-OWNERSHIP.md + 1 บรรทัดใน canopus.md + 1 handoff file ใหม่ใน from-canopus/
- harness territory rail re-read FILE-OWNERSHIP.md แล้วยังผ่านสำหรับทุก agent (ไม่มีใคร auto-fail จาก rule ใหม่)

## deadline

ไม่มี — Peat ไม่ระบุ

## known concerns Polaris ฝากไว้

1. **handoff folder ของ Canopus ยังไม่มี** · `.claude/handoffs/from-canopus/` ไม่ exist Canopus ต้อง `mkdir -p` เอง — handoffs/README.md บรรทัด 22 ระบุว่ามีโฟลเดอร์นี้ในโครงสร้าง แต่ filesystem ยังไม่สร้าง
2. **hook infrastructure ยังไม่ครบ** · ไฟล์ `pre-handoff.sh`, `sign-work.sh` ที่ template อ้างถึง — Polaris ไม่ได้ check ว่ามีอยู่จริงหรือยัง ถ้ายังไม่มี Canopus dispatch บอกในผลลัพธ์ จะได้ทราบสภาพ harness จริงๆ
3. **STATUS.md ยังไม่มี** · Polaris จะสร้าง `docs/team/STATUS.md` ตอนปิด TASK-2026-05-14-02 (รวมประวัติ 01 ย้อนหลังด้วย)

---

*Polaris (α-OPS-00) — 2026-05-14*
