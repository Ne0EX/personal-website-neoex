# TASK-2026-05-15-50 · NETRA chat API route shell

> From · Polaris (α-OPS-00)
> To · Altair (α-BND-02)
> Date · 2026-05-15
> Priority · M (unblocks TASK-51 Arcturus + TASK-52 Sirius)

## Assignment

Build `app/api/chat/route.ts` — the POST endpoint shell for NETRA chat.

- POST endpoint accepting chat messages array
- Input validation (zod) on incoming payload
- Rate limit: 50 msg / 24h / session, keyed on `wl_session` cookie (HttpOnly, SameSite=Lax, 30d)
- Streaming response via Vercel AI SDK `streamText` — Arcturus fills the model/tools/prompt interior
- Tool-call hook points as clearly marked TODO stubs
- Refusal hook points as clearly marked TODO stubs
- Error handling + consistent error shape
- `X-NETRA-Remaining` response header with remaining quota

## Context

PRD-05 §rate-limiting + §streaming. TASK-11 S1+S2+S3 have closed (NETRA spec). TASK-22 (velite) is in flight — tools stubs reference lib/content but do not import it yet (Arcturus wires them in TASK-51).

## Scope boundaries

- Do NOT write the system prompt — Arcturus (TASK-51)
- Do NOT define actual tool execute functions — Arcturus
- Do NOT touch chat UI — Betelgeuse/Sirius
- Touch only: app/api/chat/route.ts, lib/server/rate-limit.ts

## Dependencies

- `ai` + `@ai-sdk/anthropic` — must be installed (not yet in package.json)

## Blocks

- TASK-51 (Arcturus — prompts + tools)
- TASK-52 (Sirius — chat drawer UI)
