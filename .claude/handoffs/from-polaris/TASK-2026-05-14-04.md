---
task_id: TASK-2026-05-14-04
from: polaris
to: canopus
date: 2026-05-14
priority: high
parent_directive: Peat — "ไปเคลียร์ task ต่อไปให้เรียบร้อย" (next-task candidate from STATUS.md known-gaps)
---

# TASK-2026-05-14-04 · Deploy the 6 remaining hook scripts

## scope

`.claude/hooks/README.md` ships full bash source for six hooks but only `on-dispatch.sh` and `session-start.sh` exist as deployed files. The other six are spec-only. Effect: every TASK from -01 through -03 has been signed `unsigned · sign-work.sh not deployed`. This task closes that gap so the signed-workflow contract becomes real.

The six scripts to deploy, all under `.claude/hooks/`:

1. `pre-task.sh` — agent context gate; verifies assignment + required reads
2. `harness-check.sh` — runs every configured rail; non-blocking, reports
3. `post-edit.sh` — lint + typecheck + build after any Write/Edit
4. `visual-diff.sh` — UI-change screenshot capture + Betelgeuse gate
5. `sign-work.sh` — writes v2 signature per `.claude/signatures/SCHEMA.md`
6. `pre-handoff.sh` — verifies signature + handoff template before ship

## PRD reference

No PRD covers harness internals. `.claude/hooks/README.md` is the spec. `.claude/signatures/SCHEMA.md` is the signature contract.

## slices

### S1 · Canopus — deploy the 6 hook scripts

**input**
- `.claude/hooks/README.md` — full source for all 6 scripts in fenced code blocks (§1 through §6)
- `.claude/signatures/SCHEMA.md` — v2 signature contract; sign-work.sh must conform
- `.claude/handoffs/_template.md` — already exists; pre-handoff.sh references it
- `docs/team/FILE-OWNERSHIP.md`, `docs/team/QUALITY-BAR.md`, `.claude/AGENTS.md` — pre-task.sh reads them

**output**
- 6 executable bash scripts at `.claude/hooks/{pre-task,harness-check,post-edit,visual-diff,sign-work,pre-handoff}.sh`
- each script's content **must match the README spec byte-for-byte** (extracted from the fenced code blocks). No silent improvements, no defensive additions, no comment edits. If the README spec has a bug, write a PROPOSED REVISION handoff and stop — do not silently diverge.
- `chmod +x` on all 6
- `.claude/settings.json` wiring updated per README "Installation" section:
  - `PreToolUse` (matcher: `Write|Edit|MultiEdit`) → `pre-task.sh "$CLAUDE_TASK_ID"`
  - `PostToolUse` (matcher: `Write|Edit|MultiEdit`) → `post-edit.sh`
  - `Stop` → `sign-work.sh "$CLAUDE_TASK_ID"`
  - existing `SessionStart` block and existing `PreToolUse` block for `Agent`→`on-dispatch.sh` MUST be preserved (they coexist with the new `PreToolUse` matcher because matchers are different)

**acceptance**
- all 6 files exist, mode 755, content matches README spec exactly (diff -u against an extracted reference must be empty modulo trailing newline)
- `settings.json` is valid JSON, contains all 4 hook event types (`SessionStart`, `PreToolUse`, `PostToolUse`, `Stop`) without dropping any prior wiring
- syntax check on every script: `bash -n .claude/hooks/<name>.sh` exits 0
- smoke test: run `bash .claude/hooks/pre-task.sh TASK-2026-05-14-04 canopus` from inside this task's execution — must PASS (assignment file exists, required reads exist)
- smoke test: run `WL_AGENT=canopus WL_NEXT=polaris WL_SUMMARY="deploy 6 hook scripts" bash .claude/hooks/sign-work.sh TASK-2026-05-14-04` after staging the new files — must produce a valid signature at `.claude/signatures/TASK-2026-05-14-04--canopus.json` (use this signature for your return handoff)
- update `docs/team/STATUS.md` known-gaps section: remove the entry "6 hook scripts not deployed" (it is now fixed); leave the `.harness/worldline-harness.config.json` and signatures-from-prior-TASKs entries (out of scope here)

**start** · now

## non-goals

- **do not** implement `scripts/visual-capture.sh`. README §4 explicitly defers Playwright integration as separate Canopus work. `visual-diff.sh` already handles the missing script gracefully (prints a manual-screenshot instruction).
- **do not** create `.harness/worldline-harness.config.json`. `harness-check.sh` handles its absence (returns 0 with a warning). Rail definitions are a separate task.
- **do not** edit `.claude/hooks/README.md`. The spec stays; you're producing the artifacts the spec already describes.
- **do not** retro-sign TASK-01 / -02 / -03. Those will stay `unsigned · sign-work.sh not deployed` as historical fact. The fix applies forward only.
- **do not** modify `on-dispatch.sh` or `session-start.sh`. They already exist and are wired correctly.

## dependencies

None. All input files exist. No other agent needs to run first.

## acceptance (whole task)

- S1 complete + self-signed (this is the first task with a real signature — verify the signature payload is well-formed against SCHEMA.md v2)
- return handoff at `.claude/handoffs/from-canopus/TASK-2026-05-14-04--to-polaris.md` written via the proper template + linked signature
- STATUS.md updated to close TASK-04 and to remove the deployed-hooks gap from known-gaps

## deadline

None specified by Peat.

---

*polaris · α-OPS-00 · 2026-05-14*
